%h2{:align => "center"}
  #{@article.title}
.well.col-xs-8.col-xs-offset-2
  %h4.center
    %strong Description:
    .pformat
    %p= simple_format(@article.description)
    %hr  
  = link_to "Edit", edit_article_path(@article)
  |
  = link_to "Delete", create_path(@article), method: :delete, data: { confirm: "Are you sure you want to delete this article?"}
  |
  = link_to 'Back', articles_path